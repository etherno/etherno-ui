import styled from 'styled-components'
import {
  space,
  display,
  alignItems,
  flexDirection,
  justifyContent,
  textAlign,
  flexWrap,
} from 'styled-system'

export default styled.div`
  ${space}
  ${display}
  ${alignItems}
  ${flexDirection}
  ${justifyContent}
  ${textAlign}
  ${flexWrap}
`
